# Prometheus をデータソースとして自動登録する。
# uid は固定する (ダッシュボード JSON から参照しているため)。
apiVersion: 1

datasources:
  - name: Prometheus
    uid: prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    jsonData:
      timeInterval: 15s
